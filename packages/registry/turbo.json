{
  "extends": [
    "//"
  ],
  "tasks": {
    "generate": {
      "dependsOn": [
        "generate:entries"
      ]
    },
    "generate:entries": {
      "inputs": [
        "entries/**",
        "scripts/generate-entries.ts"
      ],
      "outputs": [
        "src/entries.generated.ts"
      ]
    }
  }
}
