---
'lsp-proxy-config-default': minor
---

Add Vue support: Volar for `.vue`, vtsls holding the TypeScript project behind
it with `@vue/typescript-plugin` loaded, and the bridge that carries Volar's
tsserver requests between them.
